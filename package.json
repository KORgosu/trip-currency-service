{
  "name": "country-ranking-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
  "build": "tsc",
  "dev": "npm run build && node dist/server.js",
    "start": "node dist/server.js",
    "start:bg": "powershell -Command \"Start-Process node -ArgumentList 'dist/server.js' -WindowStyle Hidden\"",
    "sim": "ts-node-esm scripts/simulate.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "fastify": "^4.22.0",
  "@fastify/cors": "^9.0.0",
    "fastify-plugin": "^4.5.0",
    "pino": "^8.0.0",
    "luxon": "^3.0.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
  "ts-node": "^10.9.1",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
