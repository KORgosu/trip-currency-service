# Currency Service 환경 변수 설정 예시
# 실제 사용 시 .env 파일로 복사하여 값을 수정하세요

# 환경 설정
ENVIRONMENT=local
SERVICE_VERSION=1.0.0

# 데이터베이스 설정 (MySQL/Aurora)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=currency_db
DB_USER=currency_user
DB_PASSWORD=password

# Redis 설정
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# DynamoDB 설정 (AWS 환경에서만 사용)
DYNAMODB_REGION=ap-northeast-2
SELECTIONS_TABLE=travel_destination_selections
RANKINGS_TABLE=RankingResults

# Kafka 설정
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# SQS 설정 (AWS 환경에서만 사용)
SQS_QUEUE_URL=
SQS_REGION=ap-northeast-2

# 외부 API 키
BOK_API_KEY=your_bok_api_key_here
FED_API_KEY=your_fed_api_key_here
BACKUP_API_KEY=your_backup_api_key_here

# AWS 설정 (배포 시 사용)
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# LocalStack 설정 (로컬 개발용)
AWS_ENDPOINT_URL=http://localhost:4566

# 로깅 설정
LOG_LEVEL=INFO
LOG_FORMAT=json

# 서비스 포트 설정
CURRENCY_SERVICE_PORT=8001
RANKING_SERVICE_PORT=8002
HISTORY_SERVICE_PORT=8003

# 성능 설정
REQUEST_TIMEOUT=30
MAX_RETRIES=3
CACHE_TTL=600

# 보안 설정
RATE_LIMIT_PER_MINUTE=100

# 개발 도구 설정
DEBUG=false
RELOAD=true